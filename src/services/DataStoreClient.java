package services;

import model.SpamContentData;

// A fake datastore client for storing data
public class DataStoreClient {
    public void store(SpamContentData spamContentData) {
        // execute database query to store the data
    }
}
